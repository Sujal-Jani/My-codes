#include<iostream>
using namespace std;

int power(int a,int b){
  if(b==0){
    return 1;
  }
  return a*power(a,b-1);
}

int main(){
  int a,b;
  cout<<"a : ";
  cin>>a;
  cout<<"b : ";
  cin>>b;
  
  cout<<endl<<a<<" power "<<b<<" is equal to : "<<power(a,b)<<"\n\n";
}
